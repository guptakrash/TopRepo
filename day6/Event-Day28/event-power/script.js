const form=document.querySelector('form')
//form.addEventListener('submit',()=>{
    const findPower=(a,b)=>
    const base=document.getElementById('').value
    const expo=document.getElementById('').value
}
)
    
